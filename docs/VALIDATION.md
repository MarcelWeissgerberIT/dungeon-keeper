# Validation record

Validated on 2026-09-09 using Node.js 24.

- 41 automated tests pass: room costs and rejected actions, reachable gold excavation/hauling/claiming, inaccessible mining, worker independence from rally orders, save/load at fractional positions, feeding and rest, traps during combat, combat needs and leveling, walls blocking ranged damage, recruitment/research/payroll, malformed saves, terminal outcomes, and full expeditions on relaxed/normal/hard using regular player commands.
- All three complete expeditions include a midgame save/load and end with victory using normal resources and powers.
- Independent music/effects gain and mute settings are verified with a Web Audio test double. Audio requires a user interaction, as enforced by browsers. Synthetic creature voices and music are original; perceived sound quality has not been measured by an automated listening test.
- Room, creature, room-description and dynamic-event translations are tested, including callback/key/accessibility preservation in the translated React tree and a regression test for unkeyed static siblings during English rendering.
- WebMCP read/order/pause registration and shared-state actions pass in a mocked registry, including invalid input and cleanup. The live browser also reported all three WebMCP tools registered on both development and production-preview origins. Browser WebMCP calls themselves were not exercised; the action implementation remains covered by the registry tests.
- TypeScript checking and the production build pass. Vite reports the expected large Three.js renderer chunk. Shared atlas source images avoid uploading a separate full image for each frame. Loading waits for the real atlas image before attaching GPU textures, avoiding invalid placeholder-size reuse. State replacement invalidates terrain and creature caches.
- The production build is served at the repository subpath and loads the complete OpenArt texture/sprite set, font and HUD. The final production-preview browser console has no warnings or errors.
- All twelve OpenArt images and the prepared sprite sheets were visually reviewed. Transparency defects, stray neighbouring-frame pieces and inconsistent sprite baselines were corrected before use. The final game was screenshot-reviewed in the in-app browser. UI smoke tests cover start/pause, selecting a creature, slap damage/needs feedback, grabbing/cancelling placement, German/English switching, settings and independent music mute while effects remain enabled. Audio callbacks executed without runtime errors; this is not a subjective listening review.

## Excavation / future-room designation and volumetric creatures

- The rectangle draft supports both excavation-only and future-room orders on known earth/gold or empty floor. Room selection preserves the drawn area; Enter works after mouse selection of a room, and Escape removes the draft. Terrain picking uses the visible surface instead of projecting through a raised wall onto hidden ground.
- Confirmed orders are stored per tile, drawn with coloured grids/room diamonds and displayed on the minimap and order list. A single render pass combines base grid, existing orders and the draft without exceeding one entry per map tile. Removing an order never sells a completed room.
- Six designation tests cover the complete excavation/claim/build sequence, zero-gold waiting and resumption, idempotency and repurposing, cancellation after partial completion, unreachable areas, old-save migration, rejection of invalid/array-valued plans, save/load without double charges, and a full treasury with workers carrying gold. Construction jobs can expand storage even when delivery is blocked; fully loaded workers do not keep mining more gold.
- The original immediate-construction API remains covered by its atomic-budget tests. The new HUD queues work without reserving or charging gold; each finished tile is paid for once by the worker construction path. Pending orders survive saves, while unconfirmed rectangle drafts do not.
- Browser checks include drawing a 2×2 grid containing earth, changing it from excavation to a future archive, confirming with Enter after room-button focus, observing the retained grid and order list without immediate spending, and automatic worker execution. English text and pending-order controls are also covered by the final release review.
- All five creature rigs remain finite, grounded and raycastable. Tests verify independent animation poses, shared meshes and caller-controlled transforms. Per-model rigid parts use 15–22 draw calls, a structural optimization rather than a measured frame-rate guarantee. No additional OpenArt assets or credits were required for this mechanics revision.
- Lint passes for the construction/model modules and new tests. Broader lint still reports existing UI/scaffold conventions (React effect initialization, anchor/image/status markup and a simulation `prefer-const` finding); it is not a release check in the Pages workflow.

## Practical boundaries

One replayable expedition, not a remake of another game's campaign. Resource layouts vary by seed while the starting dungeon stays learnable. Desktop mouse/keyboard is the primary input; narrow displays use a compact command column; secondary passive indicators are reduced on short windows. No multiplayer, cloud saves or imported third-party game assets. Persistent data stays in the local browser; blocked browser storage leaves the live session playable but prevents saves. The build is static and requires no server secrets.

## Direct creature handling and fresh map seeds

- Seven handling tests cover inactive held workers/scholars/fighters, enemy target exclusion, population/payroll, unclaimed disconnected landing tiles, invalid drops, repeated pickup while paused, cancel without loss/penalty, save compatibility, fresh/reproducible seeds, and capped slap damage/mood/energy/effects. Existing deterministic campaign fixtures now pass explicit seeds.
- Browser checks exercised direct click pickup/click drop, drag-and-drop with the default excavation tool, blocked earth keeping the creature in hand, Escape cancellation, and right-click slap with visible health-bar feedback. English pickup/error messages and German HUD were checked.
- Three browser-created worlds showed seeds 585385197, 2683902329 (page reload) and 3818949338 (New expedition). Loading retains the saved seed; fresh seed generation tolerates disabled storage and prevents immediate repetition even when an entropy source repeats.
- Held rigs follow the pointer and dangle; valid/invalid landing tiles use green/red highlights. Landing animation follows accelerated simulation timing while remaining animated when paused. Direct slap audio is emitted once per action through the independently controlled effects channel.

## Runenpixel HUD and complete English interface (9 September 2026)

- The active typeface is the original **Kluftkrone Runenpixel**. Its editable binary raster drawings and generator are in `scripts/build_runenpixel.py`; no existing font supplied the outlines. The delivered TTF and WOFF2 contain 223 glyphs / 212 Unicode mappings. The font specimen was rendered from the delivered TTF and visually inspected. Pinned-dependency rebuilds produce identical font hashes; the checked-in validation report records the final files.
- Three additional OpenArt images provide the menu frame, blank button and icon frame. Their prompts, generation IDs and hashes are recorded in `docs/runic-hud-generations.json`. Lossless WebP conversion preserves decoded source pixels; CSS slices preserve the corners at different HUD/menu sizes. The raw generated images were visually inspected before integration.
- Runenpixel is applied to menus, cards, counters, tooltips and dialogs in both languages. German diacritics, English letters, arrows and displayed HUD punctuation have font coverage. Core interface sizes use the font's 14/28/42 pixel grid; compact passive text uses 12 pixels.
- The English audit covers help, objectives, room and creature names, status/messages, construction hints, sound settings, save/load, end states and shortcuts. Internally generated minimap/canvas labels and dialog close controls now receive localized labels directly. Language changes update document metadata and persist across reloads, while existing German saved-event strings are translated at display time. WebMCP labels/output copies follow the selected language without mutating simulation status strings.
- All **43 tests pass**, including shortcut/audio/status translations, display-time localization of existing events, English WebMCP output, and the existing gameplay/audio/save regression suite. TypeScript and the production build pass. The existing Three.js bundle-size warning remains.
- English label widths were measured using the actual font. A 320px desktop command column and zero extra letter-spacing fit the room titles; compact gold/component icons keep defence prices legible on narrow cards. Translation and WebMCP modules/tests also pass targeted lint.
- This revision was checked through source/layout review, font metrics/specimens, automated tests, production compilation and asset/base-path checks. No new browser interaction or screenshot review was performed for this revision; the earlier browser checks above describe their respective previous releases.

## Prison, torment and ritual rooms (9 September 2026)

- **56 tests pass.** Thirteen new dark-arts regressions cover capacity reservation, common melee/trap/magic defeat processing, unreachable cells, enemy/payroll exclusions, hand/cancel/drop/saved origins, safe sale, full conversion and save continuity, interrupted/possessed/held staffing, accommodation waits, worker-built plans, ritual requirements/costs/timers/combat/mana/morale, old-save migration, corrupt imports, and English status/event coverage.
- Audio tests additionally verify that all three new cues connect to the effects channel. Creature-pose tests include the new rune-weaver ritual job. The original three complete campaign fixtures and previous excavation, handling, seed and save tests still pass.
- Independent simulation checks exercised 200 mixed capture/hand/sale/ritual actions with unique-cell and save/load invariants. Captives retain their origin while held, gain no additional defeat reward and do not block victory. Controlled rune weavers cannot perform background conversions. Save imports reject dead captives, invalid progress, duplicate cells and unbounded future blessing times.
- New room types are registered in building, queued plans, minimap colors, English text and the explicit room catalogue. New room fixtures bypass the original six-cell atlas. Conversion invalidates the cached creature model by kind/faction, so the same saved unit ID can visibly become an allied warden.
- The bounded build grid scrolls to all nine rooms. Prison status and queued orders share one bounded right-side column below the objective; this column yields to the unit inspector or active construction draft. Room icons were visually reviewed as files. No browser interaction or screenshot review was performed for this release; verification used source/layout review, font metrics, tests, compilation, and asset delivery checks.
- Targeted lint for construction/audio/i18n/WebMCP and the new dark-arts tests passes. The production build and TypeScript pass; the existing Three.js bundle-size warning remains.
